apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
  namespace: secure-tasks
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      serviceAccountName: redis-sa
      containers:
        - name: redis
          image: redis:7-alpine
          securityContext:
            privileged: false
          ports:
            - containerPort: 6379

---
apiVersion: v1
kind: Service
metadata:
  name: redis
  namespace: secure-tasks
spec:
  selector:
    app: redis
  ports:
    - port: 6379
      targetPort: 6379